# PlantItEatIt API — Environment Variables
# Copy this file to .env and fill in values
# Never commit .env to GitHub

# PostgreSQL connection string (Render managed database)
DATABASE_URL=postgresql://user:password@host:5432/plantiteatit

# Environment
NODE_ENV=development

# Port (Render sets this automatically in production)
PORT=3000
